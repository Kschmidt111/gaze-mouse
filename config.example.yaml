camera:
  index: 0
  width: 640
  height: 480
  fps_target: 30

screen:
  # primary | virtual_desktop — see docs before changing
  mode: primary

gaze:
  gain_x: 1.0
  gain_y: 1.0
  smoothing_alpha: 0.25
  max_jump_px: 120

safety:
  kill_key: escape
  face_lost_frames: 5
  on_face_lost: stop_updates

calibration:
  grid: 4x4
  samples_per_point: 100
  dwell_ms_to_start: 700

model:
  type: cnn
  crop_size: 128
  hidden: [48, 24]
  learning_rate: 0.0003
  epochs: 250
  batch_size: 16

profile: default
